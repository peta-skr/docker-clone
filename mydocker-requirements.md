# 自作コンテナランタイム mydocker 要件定義書（学習用）

> この文書は「**何を・なぜ作るか**」を定義する。
> アーキテクチャ・システムコールの選択・API設計などの「**どう作るか**」は含めない（それらは設計フェーズで行う）。

---

## 1. 概要

### 1.1 背景

`docker run` は日常的に使われているが、その一行で何が起きているかを説明できる利用者は少ない。
「コンテナは軽量な仮想マシン」という説明が広く流通しており、これは誤りである。
コンテナは仮想化された計算機ではなく、**視界を区切られ、資源を締められ、根を挿げ替えられた、ただのプロセス**である。

この誤解は使う分には害がない。しかし、コンテナが壊れたとき——プロセスが見えない、
ファイルが消える、メモリ上限が効かない——**中で何が起きているかを知らないと原因にたどり着けない**。

既存のコンテナランタイムのソースを読む道もあるが、`runc` や `containerd` は
本番運用に必要な分岐と抽象で厚く覆われており、**核となる仕組みが埋もれている**。

### 1.2 目的

**最小限のコンテナランタイムを自分の手で書き、「コンテナの正体はプロセスである」ことを体で理解する。**

到達点は次の一行が動くことである。

```
mydocker run alpine /bin/sh
```

この中で `ps` を打ったとき、**自分が PID 1 として1つだけ並ぶ**。`hostname` はホストと違う。
`ls /` にはホストのファイルではなく、取ってきたイメージの中身が並ぶ。
——この3つが同時に成り立った瞬間に、目的は達成される。

学習目的のプロジェクトであり、**収益化は行わない**。市場検証は行わない。

---

## 2. 用語定義

| 用語 | 定義 |
|------|------|
| コンテナ | ホストから隔離された視界と、上限を与えられた資源の中で動く1つ以上のプロセス。仮想機械ではない |
| イメージ | コンテナの初期状態となるファイル群一式。読み取り専用で、複数のコンテナから共有される |
| レイヤ | イメージを構成する差分の一枚。イメージは複数のレイヤを下から順に重ねたものとして表現される |
| 名前空間 | プロセスから見える資源（プロセス一覧・ホスト名・ファイルシステム等）の範囲を区切る仕組み |
| 資源制限 | プロセス群が使えるメモリ・CPU・プロセス数に上限を設ける仕組み |
| ルートファイルシステム（rootfs） | コンテナの中で `/` として見えるディレクトリ木 |
| レジストリ | イメージを配布するサーバ。取得には認証トークンが要る |
| ホワイトアウト | 上の層で「ファイルを削除した」ことを表す特殊な印。重ね合わせたとき下層のファイルを隠す |
| 重ね合わせ | 複数のレイヤを1つのディレクトリ木として見せる仕組み。書き込みは最上層だけに溜まる |
| 実行中コンテナ | 起動されてまだ終了していないコンテナ。一覧・追加コマンド実行の対象になる |

---

## 3. スコープ

### 3.1 対象範囲（MVP）

- コンテナを起動し、指定したコマンドを実行できること
- コンテナの中の視界（プロセス一覧・ホスト名・ファイルシステム）がホストと分離されていること
- コンテナが使えるメモリ・CPU・プロセス数に上限を設けられ、それが実際に効くこと
- レジストリからイメージを取得し、それを使ってコンテナを起動できること
- 複数のレイヤからなるイメージを、正しい重なり順で1つのファイルシステムとして見せられること
- 実行中のコンテナを一覧でき、そこへ追加のコマンドを実行できること
- コンテナの終了後、ホストに設定・一時ファイル・マウントが残らないこと

### 3.2 スコープ外

- イメージのビルド（Dockerfile 相当）
- レジストリへの push
- 複数コンテナのオーケストレーション
- ボリューム管理
- `docker compose` 相当
- Windows・macOS でのネイティブ動作
- rootless 実行
- seccomp・AppArmor による権限の絞り込み
- コンテナ間およびコンテナ・外部間のネットワーク通信

> スコープ外の項目は「不要」ではなく、MVP完成後に明確な目的をもって追加する発展課題とする（§7）。

---

## 4. 前提・制約

- **形態**: コマンドラインツール1本。画面（GUI・Web UI）は持たない
- **動作環境**: Linux 上でのみ動作する。ホストの資源を直接操作するため、管理者権限を必要とする。Windows・macOS では動作しないため、それらの利用者は Linux 環境を用意することが前提となる
- **技術領域**: コンテナ関連ライブラリを使わず、カーネルの機能を直接扱うことを含む。イメージの取得も、既存のコンテナ実行環境に依存せず自前で行う
- **利用者**: 開発者本人および、実装を読んで学ぶ読者。不特定多数への配布・運用は想定しない
- **環境差**: 資源制限の仕組みは環境によって利用可否と作法が異なる。特定の環境を前提にせず、利用できる方式を実行時に判定する
- **外部依存**: イメージの取得には外部ネットワークが必要である。取得元には匿名利用の回数制限があるため、取得済みのものを再利用できる必要がある

---

## 5. 機能要件

各要件は「ユーザーストーリー」と「受け入れ条件」で記述する。受け入れ条件を満たせばその要件は完了とみなす。

### FR-1 コンテナの起動とコマンドの実行

**ストーリー**: 利用者として、イメージと実行したいコマンドを指定してコンテナを起動したい。なぜならホストを汚さずに、用意された環境の中でコマンドを試したいから。

**受け入れ条件**:
- [ ] イメージ名と実行するコマンドを指定して起動できる。
- [ ] 指定したコマンドの標準入出力が、起動した端末とつながる。
- [ ] コンテナ内のコマンドが終了すると、起動したコマンドも終了する。
- [ ] 存在しないコマンドを指定した場合、エラーが表示されて異常終了する。

### FR-2 ホストから隔離された視界

**ストーリー**: 利用者として、コンテナの中から見える世界がホストと別物であってほしい。なぜならホストで動いている無関係なプロセスやファイルが見えると、隔離されているとは言えないから。

**受け入れ条件**:
- [ ] コンテナ内でプロセス一覧を取得すると、ホストで動いているプロセスが1つも表示されない。
- [ ] コンテナ内で最初に起動したプロセスの番号が 1 になる。
- [ ] コンテナ内で取得したホスト名が、ホストのホスト名と異なる。
- [ ] コンテナ内から root 直下の一覧を取ると、ホストのファイルではなくイメージの中身が表示される。

### FR-3 ホストのファイルへの到達不能

**ストーリー**: 利用者として、コンテナの中のプロセスがホストのファイルへ手を伸ばせないようにしたい。なぜなら隔離されているつもりで動かした処理がホストの資産を壊すのが最も避けたい事故だから。

**受け入れ条件**:
- [ ] コンテナ内から、起動前のファイルシステムに含まれていたディレクトリへ到達できない。
- [ ] コンテナ内で行ったマウント操作の結果が、ホスト側のマウント一覧に現れない。

### FR-4 資源の上限

**ストーリー**: 利用者として、コンテナが使えるメモリと CPU に上限を設けたい。なぜなら1つのコンテナの暴走でホスト全体が巻き添えになるのを防ぎたいから。

**受け入れ条件**:
- [ ] メモリ上限を指定して起動できる。
- [ ] 上限を超えてメモリを確保しようとしたプロセスは、確保に失敗するか強制終了される。
- [ ] CPU の使用割合に上限を指定でき、上限を超えて使い続けられない。
- [ ] コンテナの終了後、設定した制限はホストに残らない。

### FR-5 イメージの取得

**ストーリー**: 利用者として、手元に無いイメージを名前で指定するだけで使い始めたい。なぜなら使うたびにファイル一式を自分で用意するのでは、コンテナを使う意味がないから。

**受け入れ条件**:
- [ ] イメージ名を指定すると、外部から取得したうえでコンテナが起動する。
- [ ] 一度取得したイメージは、2回目以降は外部へ取りに行かずに起動できる。
- [ ] 存在しないイメージ名を指定すると、原因が分かるエラーが表示されて異常終了する。

### FR-6 取得したイメージの安全な展開

**ストーリー**: 利用者として、取得したイメージが展開先の外にファイルを作らないと保証してほしい。なぜならイメージの提供者は自分ではなく、悪意ある中身を掴む可能性があるから。

**受け入れ条件**:
- [ ] 展開先の外を指すパスを含むイメージを与えても、展開先の外にファイルが作られない。
- [ ] 展開先の外を指すパスを検出した場合、その旨が分かるエラーになる。

### FR-7 レイヤの重ね合わせ

**ストーリー**: 利用者として、複数の差分から成るイメージが1つのファイルシステムとして見えてほしい。なぜならイメージの内部構造を知らなくても中身を使えるべきだから。

**受け入れ条件**:
- [ ] 複数の差分を持つイメージから起動したとき、後の差分の内容が先の差分より優先して見える。
- [ ] 後の差分で削除されたファイルが、コンテナの中から見えない。
- [ ] コンテナ内で行ったファイルの変更が、同じイメージから起動した別のコンテナに影響しない。

### FR-8 実行中コンテナの一覧

**ストーリー**: 利用者として、いま動いているコンテナを一覧したい。なぜなら何が動いているか分からない状態では、止めることも調べることもできないから。

**受け入れ条件**:
- [ ] 実行中のコンテナが、識別子・イメージ名・実行中のコマンドとともに一覧表示される。
- [ ] 一覧に表示される件数が、実際に実行中のコンテナの数と一致する。
- [ ] 終了したコンテナは一覧に表示されない。

### FR-9 実行中コンテナへの追加コマンド実行

**ストーリー**: 利用者として、動いているコンテナの中でもう1つコマンドを走らせたい。なぜなら中で何が起きているかを、起動し直さずに調べたいから。

**受け入れ条件**:
- [ ] 実行中のコンテナの識別子を指定して、その中でコマンドを実行できる。
- [ ] 実行したコマンドからは、そのコンテナと同じ視界（プロセス一覧・ホスト名・ファイルシステム）が見える。
- [ ] 実行中でない識別子を指定すると、エラーが表示されて異常終了する。

### FR-10 終了後に何も残さないこと

**ストーリー**: 利用者として、コンテナが終わったらホストが元の状態に戻ってほしい。なぜなら使うたびに痕跡が積み上がる道具は、いずれホストを埋め尽くすから。

**受け入れ条件**:
- [ ] コンテナの終了後、設定した資源制限がホストに残らない。
- [ ] コンテナを繰り返し起動・終了しても、ホストのマウント数が増え続けない。
- [ ] 起動の途中で失敗した場合も、資源制限とマウントが残らない。
- [ ] 使用中で解放できなかった資源がある場合、削除を強行せずエラーとして報告する。

---

## 6. 非機能要件

要件（達成すべき状態）を記す。実現手段は設計フェーズで決める。

| 分類 | 要件 |
|------|------|
| 安全性 | コンテナ内のプロセスが、ホストのファイルを意図せず読み書きできない。 |
| 安全性 | 取得したイメージの中身が、展開先として指定した場所の外にファイルを作れない。 |
| 安全性 | 解放に失敗した資源があるとき、後続の削除処理へ進まない。 |
| 信頼性 | コンテナが異常終了しても、ホストに設定や一時ファイルが残らない。 |
| 信頼性 | 資源制限が利用できない環境では、黙って無視せず利用者に伝わる。 |
| 可搬性 | 同じ手順で、開発者以外の環境でも同じ結果が得られる。 |
| 可搬性 | 資源制限の仕組みが環境ごとに違っても、同じ指定方法で同じ結果が得られる。 |
| 性能 | コンテナの起動が、利用者が待たされたと感じない時間で完了する。 |
| 保守性・学習性 | 各機能を独立して確認・テストできる単位で開発できる。 |
| 保守性・学習性 | 主要な設計判断は、選択肢と理由を記録として残せる状態にする。 |
| 保守性・学習性 | 利用者が直接使わない内部専用の入り口が、利用者向けの説明に現れない。 |

---

## 7. 発展課題

§3.2 で対象外とした項目は、MVP 完成後に明確な目的をもって着手する発展課題である。

**ネットワーク通信** — コンテナから外部へ、またコンテナ同士で通信できるようにする。仮想的な線を1本作ってホスト側の橋につなぎ、外向きの通信は住所を書き換えて出す、という4段構えが必要になる。1つでも欠けると通信できず、しかも原因の切り分けが難しい。MVP から外したのはこの切り分けコストのためである。

**イメージのビルド** — 手順書からイメージを組み立てられるようにする。1手順ごとに差分を1枚作り、それを積み上げる。取得（読む側）が動いてから着手すると、レイヤの構造がすでに理解できているので進めやすい。

**レジストリへの push** — 作ったイメージを配布できるようにする。取得の逆向きで、書き込み権限を持つトークンが要る。

**rootless 実行** — 管理者権限なしでコンテナを動かせるようにする。利用者そのものを別の名前空間に置き、コンテナの中の管理者をホストの一般利用者に対応させる。MVP で管理者権限を前提にしたのは、この対応付けの難しさを先送りするためである。

**権限の絞り込み** — コンテナ内のプロセスが呼べる操作そのものに制限をかける。名前空間は「見える範囲」を区切るが、「できること」は区切らない。この差を埋めるのがこの課題である。

**ボリューム管理** — コンテナの終了後も残したいデータの置き場を用意する。MVP では「終了したら消える」ことを徹底したので、その逆の需要にどう応えるかを設計する必要がある。

**複数コンテナのオーケストレーション** — 複数のコンテナをまとめて定義・起動・停止する。ネットワークとボリュームが揃ってから着手する。

---

*この要件定義はMVPの合意事項を定める。設計・実装はここを起点に進める。*
