# 03 実装ログ — 設計判断・踏んだバグ・確認の方法

> `mydocker` を書きながら下した判断と、実際に踏んだ失敗の記録。
> **この教材は「動かない時間」が長い。** 何にどれだけ詰まったかを残しておく。

---

## 第1部 設計判断

各項目は **判断軸 / 対抗案 / 結果** の形式で書く。

---

### 判断 1. namespace の作り方 — 「自分を再実行する」か「素直に unshare する」か

**判断軸**: コンテナの最初のプロセスを **PID 1 にできるか**。

**対抗案**:

| 案 | 内容 | 却下の理由 |
|---|---|---|
| A. `unshare` してから `exec` | 1プロセスの中で `unshare(CLONE_NEWPID)` を呼び、続けて目的のコマンドを `exec` | ⚠ **PID 名前空間は `unshare` を呼んだプロセス自身には適用されない。** 適用されるのは*その後に作られた子*から。自分が PID 1 になることは原理的にできない |
| B. `fork` してから子で `unshare` | 子プロセスの中で `unshare` する | 同じ理由で子自身も PID 1 にならない。さらに Go のランタイムはマルチスレッドで、`unshare(CLONE_NEWNS)` のようなスレッド単位の操作を素直に扱えない |
| **C. re-exec パターン** | `/proc/self/exe`（＝自分自身の実行ファイル）を `init` という別のサブコマンドとして `Cloneflags` 付きで起動する | — |

**結果**: **C を採用**。`clone(2)` のフラグとして名前空間を渡すので、**子は生まれた瞬間から新しい PID 名前空間の中におり、PID 1 になる**。

実際に手で確かめた差:

```
$ unshare --pid sh -c 'echo "自分の PID: $$"'
自分の PID: 25171          ← PID 1 になっていない

$ unshare --pid --fork sh -c 'echo "自分の PID: $$"'
自分の PID: 1              ← --fork（＝子を作る）を挟んで初めて 1 になる
```

`--fork` が Go における「`/proc/self/exe` を起動する」に相当する。

**残った代償**: `init` という利用者が叩かない入口が CLI に増える。
`--help` には出さず、`src/README.md` に「内部用」と明記して落とし前をつけた。

---

### 判断 2. 親子の同期 — 起動してすぐ走らせるか、合図を待たせるか

**判断軸**: **cgroup が効く前にコンテナが走り出さない**こと。

**対抗案**:

| 案 | 却下の理由 |
|---|---|
| A. 子を起動してすぐ cgroup に入れる（同期なし） | ⚠ **競合する。** `cmd.Start()` から `cgroup.procs` への書き込みまでの数ミリ秒の間に、子は既に `exec` を終えてメモリを確保し始めている。上限テストが**気まぐれに通ったり落ちたりする** |
| B. 親を先に cgroup に入れ、子に継承させる | 親（`mydocker` 本体）まで上限に縛られる。cgroup v2 の「内部プロセス禁止」規則とも噛み合わない |
| **C. パイプで同期する** | — |

**結果**: **C を採用**。親が `os.Pipe()` を作り、読み側を `cmd.ExtraFiles` で子に渡す（子から見て **fd 3**）。
子は `Init` の先頭でこの fd を読み切るまで**一歩も動かない**。
親は cgroup への登録と `state.json` の保存を終えてから `"OK"` を書いて閉じる。

副産物として**失敗の伝達**もできた。親が準備に失敗したら、合図を書かずに閉じる。
子は `"OK"` 以外を受け取ったら何もせずに終了する。

---

### 判断 3. `chroot` か `pivot_root` か

**判断軸**: 隔離が**破られないこと**。

**結果**: **`pivot_root` を採用**。`chroot` は**抜け出す手口が知られている**。実際にやってみた:

```
chroot 直後の ls / : ['bin', 'inner']
脱獄後の  ls / : ['bin', 'bin.usr-is-merged', 'boot', 'container_info.json', 'dev', 'etc', 'home', 'lib', ...]
```

手口は単純で、**`chroot` する前にディレクトリのファイルディスクリプタを掴んでおき**、
`chroot` 後に `fchdir` でそこへ戻り、`..` を登り切ってから `chroot(".")` するだけである。
`chroot` は「`/` の見え方を変える」だけで、**プロセスが既に持っている外側への参照を無効化しない**。

`pivot_root` は古い root を `put_old` に押し出し、それを `MNT_DETACH` で**マウント木から切り離す**。
参照ごと消えるので同じ手は使えない。実際、コンテナの中に `.old_root` は残っていない:

```
$ mydocker run alpine /bin/sh -c 'ls -a /; ls /.old_root'
.
..
bin
dev
etc
...
ls: /.old_root: No such file or directory
```

---

### 判断 4. cgroup — v2 だけで書くか、v1 も持つか

**判断軸**: **この環境でメモリ上限が実際に効くか**（Must の中核）。

疎通実験（`00_smoke_test.md` §3）で分かったこと:

- `/sys/fs/cgroup` は **tmpfs** で、その下に **v1 のコントローラが個別にマウント**され、
  さらに `unified/` に **cgroup2 も同時にマウント**されている（hybrid 構成）
- その cgroup2 の `cgroup.controllers` は `cpuset hugetlb` **のみ**。
  **`memory` / `cpu` / `pids` は v1 側に取られていて、v2 からは使えない**

**対抗案**:

| 案 | 却下の理由 |
|---|---|
| A. v2 だけ実装し、この環境では cgroup を Should に降格 | INSTRUCTIONS.md §1 の判定表どおりではあるが、**v1 経由なら実際に効くのに動かさずに終わる**ことになる |
| B. v1 だけ実装 | 新しい環境（v2 単独）で動かなくなる。教材としても古い作法だけを教えることになる |
| **C. v2 優先・v1 フォールバック** | — |

**結果**: **C を採用**。起動時に判定する:

1. `/sys/fs/cgroup` と `/sys/fs/cgroup/unified` を `statfs` し、**`f_type` が `cgroup2fs`（0x63677270）** か見る
2. さらに `cgroup.controllers` を読み、**`memory` が語として含まれるか**を見る
3. 含まれれば v2、含まれなければ v1（`/sys/fs/cgroup/memory` などの存在を確認）

**判定を「マウントされているか」ではなく「コントローラが見えるか」にしたのが要点**である。
前者だけを見る実装は、この環境で `memory.max` が作られないまま静かに走る。

この判断により**縮退したものは無い**。実装コストは1ファイル分増えたが、
「設定できた」と「効いた」の差を読者に見せるという教材の核が守られた。

---

### 判断 5. ホワイトアウトの扱い — 削除するか、翻訳するか

**判断軸**: レイヤを**キャッシュとして共有できる**こと。

**対抗案**:

| 案 | 却下の理由 |
|---|---|
| A. 全レイヤを1つのディレクトリに順に展開し、`.wh.` を見たら実体を削除する | 平坦化した結果は**そのイメージ専用**になる。レイヤを他のイメージと共有できず、`digest` によるキャッシュが効かない |
| **B. レイヤごとに展開し、`.wh.` を overlayfs の削除印に翻訳する** | — |

**結果**: **B を採用**。

- `.wh.<名前>` → `<名前>` の位置に**キャラクタデバイス 0:0** を作る（`mknod`）。overlayfs はこれを「削除された」と解釈する
- `.wh..wh..opq` → そのディレクトリに **`trusted.overlay.opaque="y"`** の拡張属性を付ける

これで各レイヤは `digest` をディレクトリ名にした**独立・不変のキャッシュ**になり、
重ね合わせはカーネルに任せられる。

E2E で自作の2枚のレイヤを使って確認した（`e2e_test.py` の「6. レイヤの重ね合わせ」）:
上の層で `.wh.gone.txt` を置いたファイルが、重ね合わせ後に**見えない**。

---

### 判断 6. tar 展開のパス検証 — どこまで疑うか

**判断軸**: **展開先の外にファイルを作らせない**こと。イメージの提供者は自分ではない。

**結果**: **3段で守る**（`internal/image/extract.go` の `safeJoin`）。

1. **絶対パスを拒否**（`/etc/passwd`）
2. **`..` を含むパスを拒否**（`../../etc/passwd`）
3. **親ディレクトリの実体が展開先の外を指していないか確認**（`filepath.EvalSymlinks`）

**3 を入れた理由**が実装中に分かった。1 と 2 だけでは、次の手口が通ってしまう:

```
エントリ1: foo -> /          （シンボリックリンク。名前に .. も / も無いので 1,2 を通過）
エントリ2: foo/etc/passwd    （名前は無害。しかし書き込み先は本物の /etc/passwd）
```

E2E では**細工した tar を実際に作って投入**し、
`/tmp/mydocker-escaped-*.txt` が**作られないこと**をファイルの有無で確認している。
「弾いているはず」で済ませていない。

さらに、既存ファイルを上書きするときは**必ず一度 `os.Remove` してから作る**。
`O_TRUNC` で開くとシンボリックリンクを辿ってしまい、同じ穴が開く。

---

### 判断 7. `exec` の実装 — マウント名前空間にどう入るか

**判断軸**: **cgo を使わずに**（＝標準ライブラリだけで）実現できること。

**問題**: マウント名前空間への `setns(2)` は、**呼び出し元がマルチスレッドだと `EINVAL` で失敗する**。
Go のランタイムは常に複数のスレッドを持つので、この条件をそのままでは満たせない。
`runc` はこの一点のために **cgo で C のコンストラクタ**（`nsexec`）を書いている。

**対抗案**:

| 案 | 却下の理由 |
|---|---|
| A. cgo で C の初期化コードを書く | この教材は標準ライブラリのみという前提を崩す。読者にとっても難度が跳ね上がる |
| B. マウント名前空間には入らず、ホストから `/proc/<pid>/root` を覗く | `chroot` 相当になり、判断 3 で退けたものに戻る |
| **C. PID / UTS / IPC には `setns` で参加し、マウントだけ新しく作って同じ `merged` を根にする** | — |

**結果**: **C を採用**。

- `runtime.LockOSThread()` でスレッドを固定してから `setns` を3回呼ぶ
  （PID 名前空間への `setns` は**呼び出し元自身ではなく、この後に作る子**に効く）
- そのうえで `CLONE_NEWNS` だけを付けて `init` を起動し、**同じ `merged` を根にする**

**割り切りの中身**: `exec` したプロセスはコンテナ本体とは*別の*マウント名前空間にいる。
ただし根にしているのは**同一の overlay の `merged`** なので、**見えるファイルは完全に同一**である。
プロセス一覧もホスト名も本物の名前空間に参加している。実際の出力:

```
$ mydocker exec 0c1be9ad663a /bin/ps
PID   USER     TIME  COMMAND
    1 root      0:00 sleep 20        ← コンテナ本体の PID 1 が見えている
    8 root      0:00 /bin/sh -c ...
   15 root      0:00 ps
```

**これは「できたふり」ではないが、完全でもない。** `README.md` に既知の割り切りとして明記した。

⚠ 標準ライブラリの `syscall` パッケージには **`SYS_SETNS` が定義されていない**。
アーキテクチャ別のファイル（`setns_linux_amd64.go` / `setns_linux_arm64.go`）に番号を持ち、
`syscall.Syscall` で直接呼んでいる。

---

### 判断 8. 後片付けの順序 — 「アンマウント → 削除」を絶対に崩さない

**判断軸**: **イメージの実体を消さない**こと。

`umount` は **`EBUSY`（デバイスが使用中）で日常的に失敗する**。
コンテナのプロセスが残っている、シェルの作業ディレクトリがその中にある——それだけで起きる。

このとき「アンマウント → ディレクトリ削除」の実装が**前者のエラーを無視している**と、
`rm -rf` が**マウント越しに `lowerdir` の実体、つまりイメージの中身まで消す**。

**結果**: 次を規律にした。

- `overlay.Mount.Unmount()` は、`umount` が失敗したら `MNT_DETACH` を一度だけ試し、
  それでも駄目ならエラーを返す。さらに**成功した場合でも `IsMountPoint` で本当に外れたかを確認する**
- 呼び出し元（`container.Run` の `cleanup`、`container.Cleanup`）は、
  アンマウントが失敗したら**削除に進まずそこで戻る**。状態ファイルも残して人間が調べられるようにする
- `RemoveRecord` は削除の直前にもう一度 `isMounted` を確認する（二重の関所）

**実際に使用中の状態を作って確認した**。実行中のコンテナに対して `rm` すると:

```
$ mydocker rm 0c1be9ad663a
mydocker: コンテナ 0c1be9ad663a はまだ実行中です。先に終了させてください
```

このあと**イメージのディレクトリが無事であること**を E2E が確認している
（「rm を拒否した後もイメージの実体が無事である」）。

⚠ cgroup ディレクトリは `rm -rf` では消えない。cgroupfs は通常のファイルシステムではないので **`rmdir`** を使う。
中にプロセスが残っていれば失敗する。それが正しい挙動なので、握りつぶさずエラーとして返している。

---

### 判断 9. `/dev` をどう用意するか

**判断軸**: `/dev/null` や `/dev/zero` が使えること。ただし**ホストのディスクは触らせない**。

**結果**: `/dev` に **tmpfs（64KiB）を敷き、必要なノードだけ `mknod` で自分で作る**。
ホストの `/dev` を bind mount すると、コンテナからホストのブロックデバイスに手が届いてしまう。

作るのは `null` `zero` `full` `random` `urandom` `tty` の6つと、`/proc/self/fd` へのリンク。
作れなかったものは**警告を出して続行する**（起動できないよりはましなので）。

---

### 判断 10. マニフェストの選び方 — 「最初の項目」を取らない

`latest` のマニフェストを取ると、返ってくるのは**マニフェストリスト（OCI インデックス）**である。
`layers` が無いので「取れた JSON に `layers` が無い」と悩むことになる。

さらに、リストの中には次のような項目が混ざっている:

```json
{"digest":"sha256:f21951a6...","platform":{"architecture":"unknown","os":"unknown"},
 "annotations":{"vnd.docker.reference.type":"attestation-manifest"}}
```

これは署名の付随物であって、イメージではない。
**「最初の項目を取る」実装はこれを掴んで確実に壊れる。**

**結果**: `platform.os == runtime.GOOS && platform.architecture == runtime.GOARCH` で明示的に絞る
（`pickPlatform`）。見つからなければ、**あるものの一覧を添えて**エラーにする。

---

### 判断 11. 終了コードの翻訳 — 255 では伝わらない

メモリ上限で殺されたコンテナの終了コードが **255** になっていた。
Go の `ExitError.ExitCode()` は**シグナルで殺された場合 -1 を返す**ためで、
これをそのまま `os.Exit` に渡すと 255 になる。

これでは「OOM で殺された」ことが利用者にも E2E にも伝わらない。

**結果**: `WaitStatus.Signaled()` を見て、**128 + シグナル番号**を返すようにした（シェルの慣習）。
これで OOM kill は `137`（= 128+9）になり、テストで**明示的に検査できる**ようになった。

```
$ mydocker run --memory 10m alpine /bin/sh -c 'dd if=/dev/zero of=/dev/null bs=100M count=1'
$ echo $?
137
```

---

## 第2部 踏んだバグと、確認に使った方法

### バグ 1. 「メモリ上限が効いている」と誤認しかけた

最初に書いたテストはこうだった:

```sh
mydocker run --memory 10m alpine sh -c 'head -c 100000000 /dev/zero | wc -c'
```

結果は `100000000` が表示されて `exit=0`。「上限が効いていない」と思いかけたが、
**テストのほうが間違っていた**。`head | wc` は**ストリームを流すだけでメモリを溜めない**ので、
10MB の上限には決して当たらない。

**確認の方法を変えた**。`dd` に大きな `bs` を与えて**1回で 100MB のバッファを確保させる**:

```sh
# 対照（上限なし）
$ mydocker run alpine sh -c 'dd if=/dev/zero of=/dev/null bs=100M count=1'
1+0 records in
1+0 records out
104857600 bytes (100.0MB) copied, 0.047161 seconds, 2.1GB/s
$ echo $?
0

# 上限 10m
$ mydocker run --memory 10m alpine sh -c 'dd if=/dev/zero of=/dev/null bs=100M count=1'
（何も出力されない）
$ echo $?
137
```

**教訓**: 「効いているか」を確かめるテストは、**同じ操作を上限あり・上限なしの両方で走らせて差を見る**。
片方だけでは、失敗しているのが上限なのかテストなのか分からない。
E2E にも対照（上限なしなら成功する）を必ずペアで入れた。

### バグ 2. cgroup の親ディレクトリが残っていた

コンテナ終了後に葉（`.../mydocker/<ID>`）は消えていたが、
**親の `.../mydocker/` が残り続けていた**。E2E の「配下にディレクトリが残っていない」は通るが、
`ls /sys/fs/cgroup/memory/mydocker` が成功してしまう状態だった。

**確認の方法**: `find /sys/fs/cgroup -type d -name mydocker -exec find {} -mindepth 1 -maxdepth 1 -type d \;`
で**中身のあるものだけ**を数えるようにし、あわせて `Destroy` で親も `rmdir` するようにした。
他のコンテナが動いていれば `ENOTEMPTY` で失敗するが、それが正しいので無視してよい。

### バグ 3. dead code を3か所作っていた

「実装済み・テスト通過」でも、**CLI から呼ばれていない機能は要件未達**である。
コマンドの入口から末端まで経路を辿って確認したところ、3つ見つかった。

| 見つかったもの | 状態 | 対処 |
|---|---|---|
| `container.DefaultBase` | 定義しただけで誰も呼ばない | **削除** |
| `State.Base` | `state.json` に書いているが**誰も読まない** | **削除**（`ID` から導出できる） |
| `overlay.CountMounts` | 定義しただけ | **`mydocker info` に接続**（overlay の件数を実数で表示。リークの目視に使える） |
| `image.Image.WorkingDir` | レジストリから読んでいるが**コンテナに反映していなかった** | **実装**（`init` に位置引数で渡し、`pivot_root` 後に `chdir`） |

確認に使ったのは、関数名を全ファイルで数える素朴な走査:

```sh
for sym in $(grep -rhoP '^func (\([^)]*\) )?\K[A-Za-z_][A-Za-z0-9_]*' --include=*.go .|sort -u); do
  n=$(grep -rho "\b$sym\b" --include=*.go . | wc -l)
  [ "$n" -le 1 ] && echo "UNUSED?: $sym"
done
```

`go vet` はこれを教えてくれない（未使用の**ローカル変数**は検出するが、未使用の**関数**は検出しない）。

### バグ 4. `ps` のテストが「多すぎ」側を見ていなかった

最初、隔離の検査を「`ps` の出力が空でない」で済ませていた。
これは **`/proc` の再マウントを忘れていても通る**。ホストのプロセスがずらりと出るからである。

**確認の方法を変えた**: **行数がちょうど 2 行**（ヘッダ + `ps` 自身）であることを見る。
実際にこの差は劇的で、手で確かめると:

```
# PID 名前空間はあるが /proc を貼り直していない
$ unshare --pid --fork sh -c 'ps -e --no-headers | wc -l'
87                       ← ホストのプロセスが全部見える

# /proc を貼り直すと
$ unshare --pid --fork --mount-proc sh -c 'ps -e --no-headers | wc -l'
3
```

ホストの `ps -e` は 85 個。**87 という数字は「名前空間が効いていない」ようにしか見えないが、効いている。**
見えていたのは `/proc` の中身であって、名前空間の中身ではなかった。

### バグ 5. `ps` の PID 列をシェルスクリプトで取ろうとして壊した

検査用のシェルスクリプトで `mydocker ps | awk '{print $4}'` として PID を取ろうとしたところ、
`sleep 40` の `40` を掴んでいた。**COMMAND 列に空白が入る**ためである。

**対処**: E2E では `state.json` を直接読んで PID を取るようにした。
表示用の整形出力を機械が解析する前提にしない。

---

## 第3部 自己レビューの検査結果

INSTRUCTIONS.md §7 の項目を1つずつ、**実際に実行して**確認した。

| 検査項目 | 確認の方法 | 結果 |
|---|---|---|
| dead code | 関数名の全ファイル走査（上記バグ3） | 3件発見・すべて対処 |
| 「多すぎ」側の assert | `ps` は**行数ちょうど**、一覧は**合計 N 件**で検査 | 対応済み |
| テストの空振り | E2E が `10/10 件成功`・`合計 2 件`のように**実数を出力** | 対応済み |
| 修正の適用確認 | 修正後に `grep` で現物を確認し、E2E を再実行 | 56/56 通過 |
| コンテナ内 `ps` にホストが出ない | `ps` の**行数が 2 行ちょうど**（ホストは 85 プロセス） | ✅ |
| コンテナ内 `hostname` がホストと違う | `--hostname isolated-box` を指定して照合（ホストは `vm`） | ✅ |
| コンテナ内から `ls /` がイメージの中身 | ホストの `ls /` と**差集合を取り**、ホスト固有のものが見えないことを確認 | ✅ |
| cgroup が「効いた」こと | 上限あり／なしの**対照**で `dd` を走らせ、`137` を確認 | ✅ |
| cgroup の後片付け | `find /sys/fs/cgroup -name mydocker` 配下のディレクトリを数える | ✅ 0件 |
| overlay マウントのリーク | **10回起動して前後の件数を比較**（0 → 0） | ✅ |
| アンマウント失敗時に削除へ進まないか | **実行中のコンテナに `rm`** し、拒否されること＋**イメージが無事**なことを確認 | ✅ |
| ホストを壊していないか | ホストの `/proc/mounts` の行数を**テスト全体の前後で比較**（22 → 22） | ✅ |
| tar 展開のパス検証 | `../` と絶対パスの tar を**自分で作って投入**し、外にファイルが**作られないこと**を確認 | ✅ |
| エラー時に中途半端な状態を残さないか | 存在しないコマンドで**起動を失敗させ**、cgroup・マウント・記録が残らないことを確認 | ✅ |
| 強制終了された場合の残骸 | `mydocker` 自身を **SIGKILL** して残骸を作り、`rm` が安全に回収できることを確認 | ✅ |

`go build ./... && go vet ./...` は通る。`gofmt -l .` は空。

---

## 第4部 詰まった時間の内訳（記事の骨として）

| 何に | どれくらい | 何が起きていたか |
|---|---|---|
| 疎通実験と環境の見極め | 60分 | `stat -fc %T /sys/fs/cgroup` が `tmpfs` を返した時点で「v2 が無い」と結論しかけた。`mount \| grep cgroup` まで見て hybrid 構成だと分かった |
| cgroup が効いているかの確認 | 40分 | 上限は書けている。`memory.failcnt` も増えている。なのに死なない。**テストのほうが間違っていた**（バグ1） |
| レジストリのレイヤ取得 | 30分 | マニフェストは取れるのに `layers` が無い（インデックスだった）。次に、選んだ項目が attestation で再び `layers` が無い |
| `exec` のマウント名前空間 | 50分 | `setns(CLONE_NEWNS)` が `EINVAL`。Go がマルチスレッドだからだと分かるまで、権限やパスを疑って回り道した |
| 終了コードが 255 | 15分 | OOM で殺されているのに 137 にならない。`ExitCode()` がシグナル死で -1 を返すことに気づくまで |
| dead code の掃除 | 20分 | 「実装済み」と思っていた `WorkingDir` が、実は誰も使っていなかった |

**動いている時間より、動かない理由を探している時間のほうが長い。**
そしてその大半は、**「効いていないように見えるが実は効いている」**か、
**「効いているように見えるが実は効いていない」**のどちらかだった。
この教材の落とし穴ボックスは、そのすべてから作られている。
